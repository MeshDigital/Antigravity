<Page x:Class="SLSKDONET.Views.LibraryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:viewmodels="clr-namespace:SLSKDONET.ViewModels"
      mc:Ignorable="d" 
      d:DataContext="{d:DesignInstance Type=viewmodels:LibraryViewModel}"
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="LibraryPage">

    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Page.Resources>

    <Grid Background="#121212">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT SIDEBAR: Playlists -->
        <Border Grid.Column="0" Background="#1E1E1E" BorderBrush="#333" BorderThickness="0,0,1,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Grid Margin="20,20,20,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="My Library" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <Button Grid.Column="1" Content="🔄" 
                            Command="{Binding RefreshLibraryCommand}"
                            Background="Transparent" BorderThickness="0" Foreground="#888" Cursor="Hand"
                            FontSize="16" ToolTip="Refresh Library"
                            Padding="5"/>
                </Grid>

                <!-- Empty State for Playlist List -->
                <TextBlock Grid.Row="1" Text="No playlists found." 
                           Foreground="#888" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding AllProjects.Count}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Playlist List -->
                <ListBox Grid.Row="1" 
                         ItemsSource="{Binding AllProjects}" 
                         SelectedItem="{Binding SelectedProject}"
                         Background="Transparent"
                         BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         Padding="10">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="Border" 
                                                Padding="10,8" 
                                                CornerRadius="6" 
                                                Margin="0,2"
                                                Background="Transparent"
                                                SnapsToDevicePixels="True">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#333333"/>
                                            </Trigger>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#252525"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Foreground" Value="#E0E0E0"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Icon placeholder -->
                                <Border Width="40" Height="40" CornerRadius="4" Background="#2A2A2A" Margin="0,0,12,0">
                                    <TextBlock Text="🎵" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding SourceTitle}" FontWeight="SemiBold" FontSize="14" TextTrimming="CharacterEllipsis"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding SourceType}" FontSize="11" Foreground="#88FFFFFF" Margin="0,2,8,0"/>
                                        <TextBlock Text="{Binding TotalTracks, StringFormat='{}{0} tracks'}" FontSize="11" Foreground="#66FFFFFF" Margin="0,2,0,0"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- MAIN CONTENT AREA -->
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header & Toolbar -->
                <RowDefinition Height="*"/>    <!-- Track List -->
            </Grid.RowDefinitions>

            <!-- EMPTY STATE -->
            <Grid Grid.RowSpan="2" Panel.ZIndex="0" 
                  Visibility="{Binding SelectedProject, Converter={StaticResource NullToVisibilityConverter}, ConverterParameter=Inverse}">
                <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Opacity="0.5">
                    <TextBlock Text="🎵" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                    <TextBlock Text="Select a playlist to view tracks" FontSize="18" Foreground="#888"/>
                </StackPanel>
            </Grid>

            <!-- ACTIVE CONTENT -->
            <Grid Grid.RowSpan="2"
                   Visibility="{Binding SelectedProject, Converter={StaticResource NullToVisibilityConverter}}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER & ACTIONS -->
                <Border Grid.Row="0" Padding="30,25,30,20" Background="#181818">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <!-- Title & Stats -->
                        <StackPanel>
                            <TextBlock Text="{Binding SelectedProject.SourceTitle}" 
                                       FontSize="28" FontWeight="Bold" Foreground="White"
                                       TextTrimming="CharacterEllipsis"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding SelectedProject.SourceType}" Foreground="#AAA" Margin="0,0,15,0"/>
                                <TextBlock Text="•" Foreground="#555" Margin="0,0,15,0"/>
                                <TextBlock Foreground="#AAA">
                                    <Run Text="{Binding SelectedProject.SuccessfulCount, Mode=OneWay}"/>
                                    <Run Text="/"/>
                                    <Run Text="{Binding SelectedProject.TotalTracks, Mode=OneWay}"/>
                                    <Run Text="downloaded"/>
                                </TextBlock>
                            </StackPanel>
                        </StackPanel>

                        <!-- ACTIONS TOOLBAR (New Plugin System) -->
                        <ItemsControl Grid.Column="1" ItemsSource="{Binding AvailableActions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.ExecuteActionCommand, RelativeSource={RelativeSource AncestorType=Page}}" 
                                            CommandParameter="{Binding}"
                                            Margin="8,0,0,0" Padding="12,8" Background="#262626" BorderThickness="0">
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border x:Name="BtnBorder" Background="{TemplateBinding Background}" CornerRadius="4">
                                                                <StackPanel Orientation="Horizontal">
                                                                    <TextBlock Text="{Binding IconGlyph}" Margin="0,0,8,0" FontSize="14"/>
                                                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="White"/>
                                                                </StackPanel>
                                                            </Border>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter TargetName="BtnBorder" Property="Background" Value="#353535"/>
                                                                </Trigger>
                                                                <Trigger Property="IsPressed" Value="True">
                                                                    <Setter TargetName="BtnBorder" Property="Background" Value="#404040"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>

                <!-- TRACK LIST DATAGRID -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding CurrentProjectTracks}"
                          AutoGenerateColumns="False"
                          Background="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HorizontalGridLinesBrush="#222"
                          RowBackground="Transparent"
                          Foreground="#DDD"
                          RowHeight="40"
                          HeadersVisibility="Column"
                          Padding="10">
                    
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="#888"/>
                            <Setter Property="Padding" Value="10,8"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            <Setter Property="BorderBrush" Value="#333"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="DataGridCell">
                                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                        <Style TargetType="DataGridRow">
                            <EventSetter Event="PreviewMouseLeftButtonDown" Handler="DataGridRow_PreviewMouseLeftButtonDown"/>
                            <EventSetter Event="Drop" Handler="DataGridRow_Drop"/>
                            <EventSetter Event="PreviewDragOver" Handler="OnPreviewDragOver"/>
                            <Setter Property="Background" Value="Transparent"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#1AFFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#2AFFFFFF"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Resources>

                    <DataGrid.Columns>
                        <!-- # -->
                        <DataGridTextColumn Header="#" Binding="{Binding SortOrder}" Width="40" Foreground="#666"/>
                        
                        <!-- Title -->
                        <DataGridTemplateColumn Header="Title" Width="2*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Title}" FontWeight="Medium" Foreground="White" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Artist -->
                        <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="1.5*"/>

                        <!-- Album -->
                        <DataGridTextColumn Header="Album" Binding="{Binding Model.Album}" Width="1.5*" />

                        <!-- Status -->
                        <DataGridTemplateColumn Header="Status" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Left" Background="#22FFFFFF">
                                        <TextBlock Text="{Binding State}" FontSize="11" Foreground="#CCC"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- Progress -->
                         <DataGridTemplateColumn Header="Progress" Width="100">
                             <DataGridTemplateColumn.CellTemplate>
                                 <DataTemplate>
                                     <Grid Height="4">
                                         <ProgressBar Value="{Binding Progress, Mode=OneWay}" Maximum="100" Height="4" BorderThickness="0" Background="#333" Foreground="#4CAF50"/>
                                     </Grid>
                                 </DataTemplate>
                             </DataGridTemplateColumn.CellTemplate>
                         </DataGridTemplateColumn>

                    </DataGrid.Columns>
                </DataGrid>
                
            </Grid>
        </Grid>
    </Grid>
</Page>